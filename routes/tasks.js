"use strict";

const router = require('express').Router();

router.get('/', (req, res, next) => {/*TODO*/});
router.get('/:id', (req, res, next) => {/*TODO*/});
router.post('/', (req, res, next) => {/*TODO*/});
router.put('/', (req, res, next) => {/*TODO*/});
router.delete('/', (req, res, next) => {/*TODO*/});

module.exports = router;
